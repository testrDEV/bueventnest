%ul.nav.nav-tabs
  %li.nav-item.nav-dark
  = link_to 'Create Events', new_event_path, class: "nav-link active"
  %li.nav-item
  = link_to 'Show events', events_path(@event), class: "nav-link active"

    
%h2.text-center.my-3 Home

-@events.each do |event_id|
  -@event = Event.find(event_id)
  .bg-light.my-3.p-3.rounded-lg 
    %h3.text-center= link_to "#{@event.name}", event_path(@event)
  
%div
%h4.text-center.my-5
= link_to 'Click here to RSVP', events_path