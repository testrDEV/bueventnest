%ul.nav.nav-tabs
  %li.nav-item
  = link_to 'Create Events', new_event_path(@event), class: "nav-link active"
  %li.nav-item
  = link_to 'Show events', events_path, class: "nav-link active"
%h2.text-center.mb-3 Looking for an event to join, RSVP now!

// .col-mt-3
//   .row.md-3
//     = form_tag events_path, :method => :get do 
//       .form-group
//         .row-mt-3
//           = select :event, :sort, options_for_select(Event.sort_criteria, :selected => @sort), {}, :name => 'sort', :class => 'form-control mb-2', :id => 'sort_criteria'
//         .row-mt-3  
//           = submit_tag 'Sort', :class => 'btn btn-secondary btn-outline-success btn-block btn-lg rounded form-control', :id => 'sort_submit'


- @events.each do |event|
  .row.bg-light.my-5.p-3.rounded-lg
    .col-md-10
      %h3= link_to "#{event.name}", event_path(event)
      %p= event.description